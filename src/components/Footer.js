import React from 'react';
import borderImg from '../assets/images/border.png';

const Footer = () => (
  <footer id="footer" style={{ position: 'fixed', bottom: 0, display: 'flex' }}>
    <img src={borderImg} alt="Home with sign in yard" />
    <img src={borderImg} alt="Home with sign in yard" />
  </footer>
);

export default Footer;
